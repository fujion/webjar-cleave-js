{
  "name": "${project.artifactId}",
  "version": "${version.unrevise}",
  "description": "${project.description}",
  "scripts": {
    "postinstall": "gulp"
  },
  "dependencies": {
    "cleave.js": "${version.unrevise}"
    },
  "devDependencies": {
    "gulp": "^4.0.0"
  }
}
